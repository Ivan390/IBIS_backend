<?php
$postTags = ("filename1 : tag1", "filename2 : tag2", "filename3 : tag3" );
$filecount  = count($_FILES['ibismedia']['name']);
foreach ($postTags as $tag){
	for ($d=0; $d <= $filecount; $d++){
	// i want to find uploaded files that do not have a tag associated with it
	}
}

?>
